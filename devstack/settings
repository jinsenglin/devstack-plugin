# Turn on all the template services by default
enable_service mypanel

# Default options
MYPANEL_REPO=${MYPANEL_REPO:-NOTHING}
MYPANEL_REPO_BRANCH=${MYPANEL_REPO_BRANCH:-NOTHING}
MYPANEL_REPO_DIR_NAME=${MYPANEL_REPO_DIR_NAME:-NOTHING}
MYPANEL_ENABLED_DIR=${MYPANEL_ENABLED_DIR:-NOTHING}
